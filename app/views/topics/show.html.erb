<p id="notice"><%= notice %></p>

<h2>Forum</h2>
<ul>
  <% @topic.post_wrappers.each do |pw| %>
    <li><%= pw.post.title %> Offset: <%= pw.offset %>
      <%= link_to 'Reply', new_post_path(:topic_id => @topic.id, :post_id => pw.post.id) %></li>
  <% end %>
</ul>

<%= link_to 'Back', topics_path, class: "btn-flat"%>
